---
page_title: "sedai_group_priority Resource - terraform-provider-sedai"
subcategory: ""
description: |-
  Manages relative priority ordering for a set of Sedai groups. Priority decides which group's settings take precedence when a resource belongs to multiple groups (lower number = higher priority; 1 is highest). All referenced groups must be enabled, otherwise the backend will reject the assignment.
---

# sedai_group_priority (Resource)

Manages relative priority ordering for a set of Sedai groups. Priority decides which group's settings take precedence when a resource belongs to multiple groups (lower number = higher priority; 1 is highest). All referenced groups must be enabled, otherwise the backend will reject the assignment.

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `group_priorities` (Block List) Repeatable block — each entry assigns a priority to one group. The order of blocks does not matter; the `priority` value is what decides the ordering. (see [below for nested schema](#nestedblock--group_priorities))

### Read-Only

- `id` (String) Composite ID = comma-separated group IDs managed by this resource. Used for import and identification in state.

<a id="nestedblock--group_priorities"></a>
### Nested Schema for `group_priorities`

Required:

- `group_id` (String) The Sedai group ID. Typically `sedai_group.<name>.id`. The referenced group must be enabled.
- `priority` (Number) Priority value. 1-based, where 1 is the highest priority. Must be >= 1.



