---
page_title: "sedai_account_settings Resource - terraform-provider-sedai"
subcategory: ""
description: |-
  Manages the top-level settings for a Sedai account. Applies as the baseline default for every resource in the account. Only attributes specified here are tracked for drift — unmanaged per-resource-type tuning is preserved as-is.
---

# sedai_account_settings (Resource)

Manages the top-level settings for a Sedai account. Applies as the baseline default for every resource in the account. Only attributes specified here are tracked for drift — unmanaged per-resource-type tuning is preserved as-is.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) The Sedai account ID to configure. Typically `sedai_account.<name>.id`.
- `availability_mode` (String) Availability mode. Valid values: `DATA_PILOT`, `CO_PILOT`, `AUTO`.
- `optimization_mode` (String) Optimization mode. Valid values: `DATA_PILOT`, `CO_PILOT`, `AUTO`.

### Optional

- `app_settings` (Block, Optional) Per-resource-type settings for generic application workloads. Modes are limited to `DATA_PILOT` and `CO_PILOT` per spec — for AUTO, use a more specific block (kube_app_settings, ecs_app_settings, …). (see [below for nested schema](#nestedblock--app_settings))
- `bucket_settings` (Block, Optional) Per-resource-type settings for S3 buckets. Buckets do not support AUTO for optimization — only DATA_PILOT and CO_PILOT. Availability is always DATA_PILOT backend-side and not configurable here. (see [below for nested schema](#nestedblock--bucket_settings))
- `container_app_settings` (Block, Optional) Per-resource-type settings for generic containerized workloads (containers running outside Kubernetes or ECS). Allows the full mode set including AUTO. For workload-specific tuning, use kube_app_settings or ecs_app_settings. (see [below for nested schema](#nestedblock--container_app_settings))
- `ecs_app_settings` (Block, Optional) Per-resource-type settings for AWS ECS service workloads. (see [below for nested schema](#nestedblock--ecs_app_settings))
- `kube_app_settings` (Block, Optional) Per-resource-type settings for Kubernetes workloads (Deployments, StatefulSets, etc.). Every field is optional; only the fields set here are tracked for drift. Unset fields leave the backend's existing kubeSettings values untouched, including UI-configured advanced tuning not exposed in this block. (see [below for nested schema](#nestedblock--kube_app_settings))
- `sedai_sync_enabled` (Boolean) When true, Sedai auto-syncs the account's resources with the latest configuration. Defaults to false if omitted.
- `serverless_settings` (Block, Optional) Per-resource-type settings for AWS Lambda functions. `optimization_mode` is restricted to DATA_PILOT and AUTO (no CO_PILOT). `concurrency_mode` is Lambda-specific (OFF or AUTO). (see [below for nested schema](#nestedblock--serverless_settings))
- `volume_settings` (Block, Optional) Per-resource-type settings for AWS EBS volumes. Both `availability_mode` and `optimization_mode` are restricted to `DATA_PILOT` and `CO_PILOT` per spec — no AUTO for volumes. (see [below for nested schema](#nestedblock--volume_settings))

<a id="nestedblock--app_settings"></a>
### Nested Schema for `app_settings`

Optional:

- `availability_mode` (String) How Sedai manages availability.
- `horizontal_scaling_enabled` (Boolean) Enable horizontal scaling (replica count tuning).
- `horizontal_scaling_max_replicas` (Number) Ceiling for replica count when horizontal scaling is enabled.
- `horizontal_scaling_min_replicas` (Number) Floor for replica count when horizontal scaling is enabled.
- `is_prod` (Boolean) Mark this scope's apps as production. Non-prod allows more aggressive optimization.
- `optimization_mode` (String) How Sedai manages optimization.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view synced with cloud state.


<a id="nestedblock--bucket_settings"></a>
### Nested Schema for `bucket_settings`

Optional:

- `optimization_mode` (String) How Sedai manages optimization for S3 buckets. Valid: `DATA_PILOT`, `CO_PILOT`. No AUTO.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view of S3 buckets synced with cloud state.


<a id="nestedblock--container_app_settings"></a>
### Nested Schema for `container_app_settings`

Optional:

- `autonomous_action_without_traffic` (Boolean) Allow Sedai to optimize even without recent traffic data.
- `availability_mode` (String) How Sedai manages availability.
- `horizontal_scaling_enabled` (Boolean) Enable horizontal scaling (replica count tuning).
- `horizontal_scaling_max_replicas` (Number) Ceiling for replica count when horizontal scaling is enabled.
- `horizontal_scaling_min_replicas` (Number) Floor for replica count when horizontal scaling is enabled.
- `is_prod` (Boolean) Mark this scope's apps as production. Non-prod allows more aggressive optimization.
- `max_cpu_increase_pct` (Number) Guardrail: maximum acceptable CPU increase % during optimization.
- `max_latency_increase_pct` (Number) Guardrail: maximum acceptable latency increase % during optimization.
- `max_memory_increase_pct` (Number) Guardrail: maximum acceptable memory increase % during optimization.
- `optimization_focus` (String) What to optimize for. Valid: `COST`, `DURATION`, `COST_AND_DURATION`.
- `optimization_mode` (String) How Sedai manages optimization.
- `predictive_scaling_enabled` (Boolean) Enable predictive scaling based on detected seasonality.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view synced with cloud state.
- `vertical_scaling_enabled` (Boolean) Enable vertical scaling (CPU / memory right-sizing).


<a id="nestedblock--ecs_app_settings"></a>
### Nested Schema for `ecs_app_settings`

Optional:

- `availability_mode` (String) How Sedai manages availability.
- `horizontal_scaling_enabled` (Boolean) Enable horizontal scaling (replica count tuning).
- `horizontal_scaling_max_replicas` (Number) Ceiling for replica count when horizontal scaling is enabled.
- `horizontal_scaling_min_replicas` (Number) Floor for replica count when horizontal scaling is enabled.
- `horizontal_scaling_replica_increment` (Number) Fixed number of tasks added or removed per scaling event.
- `is_prod` (Boolean) Mark this scope's apps as production. Non-prod allows more aggressive optimization.
- `max_cpu_increase_pct` (Number) Guardrail: max CPU increase %.
- `max_latency_increase_pct` (Number) Guardrail: max latency increase %.
- `max_memory_increase_pct` (Number) Guardrail: max memory increase %.
- `optimization_focus` (String) What to optimize for. Valid: `COST`, `DURATION`, `COST_AND_DURATION`.
- `optimization_mode` (String) How Sedai manages optimization.
- `predictive_scaling_enabled` (Boolean) Enable predictive scaling.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view synced with cloud state.
- `service_autoscaling_enabled` (Boolean) Enable ECS service autoscaling.
- `vertical_scaling_enabled` (Boolean) Enable vertical scaling (CPU / memory right-sizing).
- `vertical_scaling_min_cpu` (Number) Floor for CPU per task. ECS units (1024 = 1 vCPU).
- `vertical_scaling_min_memory` (Number) Floor for memory per task, in MB.


<a id="nestedblock--kube_app_settings"></a>
### Nested Schema for `kube_app_settings`

Optional:

- `availability_mode` (String) Override availability mode for Kubernetes workloads. Valid: `DATA_PILOT`, `CO_PILOT`, `AUTO`.
- `horizontal_scaling_enabled` (Boolean) Enable horizontal scaling (replica count tuning) for Kubernetes workloads.
- `horizontal_scaling_max_replicas` (Number) Ceiling for replica count when horizontal scaling is enabled.
- `horizontal_scaling_min_replicas` (Number) Floor for replica count when horizontal scaling is enabled.
- `horizontal_scaling_replica_multiplier` (Number) Factor by which replicas scale during horizontal events. Kubernetes-specific.
- `is_operation_allowed` (Boolean) Master on/off for Sedai actions on Kubernetes workloads in this scope.
- `is_prod` (Boolean) Mark Kubernetes workloads as production — production allows fewer aggressive optimizations.
- `max_cpu_increase_pct` (Number) Guardrail: maximum acceptable CPU increase % during optimization.
- `max_latency_increase_pct` (Number) Guardrail: maximum acceptable latency increase % during optimization.
- `max_memory_increase_pct` (Number) Guardrail: maximum acceptable memory increase % during optimization.
- `optimization_focus` (String) What to optimize for. Valid: `COST`, `DURATION`, `COST_AND_DURATION`.
- `optimization_mode` (String) Override optimization mode for Kubernetes workloads. Valid: `DATA_PILOT`, `CO_PILOT`, `AUTO`.
- `predictive_scaling_enabled` (Boolean) Enable predictive scaling based on detected seasonality.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view of Kubernetes workloads synced with cloud state.
- `vertical_scaling_enabled` (Boolean) Enable vertical scaling (CPU / memory right-sizing) for Kubernetes workloads.
- `vertical_scaling_min_cpu_cores` (Number) Floor for CPU per container, in cores (fractional). Kubernetes-specific.
- `vertical_scaling_min_memory_bytes` (Number) Floor for memory per container, in bytes. Kubernetes-specific.


<a id="nestedblock--serverless_settings"></a>
### Nested Schema for `serverless_settings`

Optional:

- `availability_mode` (String) How Sedai manages Lambda availability. Valid: `DATA_PILOT`, `CO_PILOT`, `AUTO`.
- `concurrency_mode` (String) Whether Sedai manages Lambda reserved concurrency. Valid: `OFF`, `AUTO`.
- `max_cost_change_pct` (Number) Guardrail: maximum acceptable cost increase % when optimizing for performance.
- `max_latency_change_pct` (Number) Guardrail: maximum acceptable latency increase % when optimizing for cost.
- `optimization_focus` (String) What to optimize for. Valid: `COST`, `DURATION`, `COST_AND_DURATION`.
- `optimization_mode` (String) How Sedai manages Lambda optimization. Valid: `DATA_PILOT`, `AUTO`. No CO_PILOT for Lambda.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view of Lambda functions synced with cloud state.


<a id="nestedblock--volume_settings"></a>
### Nested Schema for `volume_settings`

Optional:

- `availability_mode` (String) How Sedai manages volume availability. Valid: `DATA_PILOT`, `CO_PILOT`. No AUTO.
- `optimization_mode` (String) How Sedai manages volume optimization. Valid: `DATA_PILOT`, `CO_PILOT`. No AUTO.
- `sedai_sync_enabled` (Boolean) Keep Sedai's view of EBS volumes synced with cloud state.



